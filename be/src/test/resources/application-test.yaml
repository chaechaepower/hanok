spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create-drop   # 테스트 시작 시 테이블 생성, 종료 시 삭제
    properties:
      hibernate:
        format_sql: true
  show-sql: true

jwt:
  secret: "테스트용시크릿키는아무거나길게작성해주시면됩니다"
  expiration: 3600000
  refreshExpiration: 1209600000

livekit:
  url: ws://hanok-livekit:7880
  api-key: ${LIVEKIT_API_KEY:testkey}
  api-secret: ${LIVEKIT_API_SECRET:testsecretkeythatisatleast32characters}